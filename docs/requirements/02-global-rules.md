# Codex One 全局交互规则

这份文档不是写某一个页面，而是写全局统一规则。你线下补需求时，优先把这里补清楚，后面每个模块就不容易打架。

## 1. 导航规则

### 当前底部导航

- 广场
- 圈子
- 消息
- 我的

### 需要你确认

- 默认进入哪个 Tab:
- 未登录时哪些 Tab 可见:
- 未认证用户能否直接进入全部 Tab:
- 从二级页面返回时，是否回到原来的 Tab:

## 2. 用户状态规则

建议先统一这几类用户状态。

- 游客
- 已注册未登录
- 已登录未认证
- 已完成手机号认证
- 已完成实名认证
- 已完成本人头像或人脸认证
- 受限用户

### 需要你确认

- 各状态分别能做什么:
- 哪些能力要限制:
- 哪些页面只提示不拦截:

## 3. 通用页面状态

每个页面默认都要考虑下面 6 种状态。

- 首次加载
- 加载完成
- 空状态
- 加载失败
- 无权限
- 审核中

### 需要你确认

- 每种状态的文案:
- 是否允许重试:
- 空状态是否给引导按钮:

## 4. 通用交互形式

### 常用交互

- 点击
- 长按
- 上拉加载更多
- 下拉刷新
- 底部弹层
- 全屏页面
- 弹窗确认

### 需要你确认

- 发布页用全屏还是弹层:
- 筛选页用抽屉还是独立页:
- 删除类动作是否二次确认:

## 5. 通用输入和校验

建议所有输入类页面统一补这几项。

- 必填项
- 字数限制
- 格式限制
- 敏感词
- 图片和视频数量限制
- 上传失败重试

### 需要你确认

- 各类字段上限:
- 哪些字段允许为空:
- 敏感词是否只提示还是直接禁止:

## 6. 审核和风控

当前你已经明确需要实名和本人认证，所以这里建议先留出全局规则。

### 需要你确认

- 头像是否先发后审:
- 视频介绍是否先发后审:
- 动态内容是否先发后审:
- 审核失败后如何提示:
- 是否允许用户重新提交:

## 7. 设计统一项

### 当前方向

- 男性用户主色偏社交感
- 女性用户主色偏女性化

### 需要你确认

- 默认中性色方案:
- 字体风格:
- 圆角大小:
- 卡片风格:
- Banner 风格:

## 8. 技术交接说明

如果你对某一条暂时拿不准，直接写:

- `待定`
- `先按默认`
- `后续补充`

我拿到以后会先按默认实现，并在交付时明确标注假设。
