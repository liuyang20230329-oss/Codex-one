name: codex_one
description: Social mobile app starter scaffold for Codex One.
publish_to: "none"

# SemVer pre-release for the March 27, 2026 auth starter.
version: 0.1.0-dev.1+20260327

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
