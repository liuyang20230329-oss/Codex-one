name: codex_one
description: Thirty-Seven Degrees mobile app scaffold with brand, trust flow, and chat MVP.
publish_to: "none"

# SemVer pre-release for the March 29, 2026 local-api and chat upgrade.
version: 0.6.0-dev.1+2026032902

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_auth: ^6.3.0
  firebase_core: ^4.6.0
  dio: ^5.4.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  bloc: ^8.1.0
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  web_socket_channel: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  bloc_test: ^9.1.3

flutter:
  uses-material-design: true
