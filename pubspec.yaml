name: codex_one
description: Thirty-Seven Degrees mobile app scaffold with brand, trust flow, and chat MVP.
publish_to: "none"

# SemVer pre-release for the March 29, 2026 brand and trust-flow refresh.
version: 0.5.0-dev.1+2026032901

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_auth: ^6.3.0
  firebase_core: ^4.6.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
